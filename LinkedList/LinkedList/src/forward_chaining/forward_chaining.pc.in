prefix=@CMAKE_INSTALL_PREFIX@
exec_prefix=${prefix}/bin
libdir=${prefix}/lib
sharedlibdir=${prefix}/lib
includedir=${prefix}/include

Name: forward_chaining
Description: forward chaining library

Requires:
Libs: -L${libdir} -L${sharedlibdir}
Cflags: -I${includedir}
